{
    "id": "gstd",
    "version": "0.1.0",
    "name": "GStreamer Daemon",
    "description": "Add GStreamer Daemon (gstd) to a devcontainer",
    "dependsOn": {
        "./src/gstreamer": {
            "gstVersion": "1.26"
        }
    },
    "options": {
        "version": {
            "type": "string",
            "description": "The gstreamer daemon version or git ref to build"
        },
        "extraArgs": {
            "type": "string",
            "description": "Any additional meson args to pass to the configuration step"
        }
    }
}
